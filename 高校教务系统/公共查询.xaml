<Window x:Class="高校教务系统.公共查询"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="公共查询" Height="350" Width="525">
    <Grid>
        <Button x:Name="TButton" Content="教师开课查询" Margin="150,73,204,0" VerticalAlignment="Top" Background="{x:Null}" Foreground="#FF007ACC" BorderBrush="#FF007ACC" BorderThickness="1" FontSize="16" Click="TButton_Click" Height="24"/>
        <Button x:Name="BButton" Content="班级课表查询" Margin="150,122,204,0" VerticalAlignment="Top" Background="{x:Null}" Foreground="#FF007ACC" BorderBrush="#FF007ACC" BorderThickness="1" FontSize="16" Click="BButton_Click" Height="24"/>
        <Button x:Name="DButton" Content="地区学生统计" Margin="150,174,204,0" VerticalAlignment="Top" Background="{x:Null}" Foreground="#FF007ACC" BorderBrush="#FF007ACC" BorderThickness="1" FontSize="16" Click="DButton_Click" Height="24"/>
        <Button x:Name="BackButton" Content="返回" Margin="66,271,360,0" VerticalAlignment="Top" Background="{x:Null}" Foreground="#FF007ACC" BorderBrush="#FF007ACC" BorderThickness="1" FontSize="16" Click="BackButton_Click" Height="24"/>

    </Grid>
</Window>
