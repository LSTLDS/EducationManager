<Window x:Class="高校教务系统.班级课表查询"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="班级课表查询" Height="350" Width="525">
    <Grid>
        <Button x:Name="BackButton" Content="返回" Margin="66,271,360,0" VerticalAlignment="Top" Background="{x:Null}" Foreground="#FF007ACC" BorderBrush="#FF007ACC" BorderThickness="1" FontSize="16" Click="BackButton_Click" Height="24"/>
        <DataGrid AutoGenerateColumns="True" x:Name="View" HorizontalAlignment="Left" Margin="43,62,0,0" VerticalAlignment="Top" RenderTransformOrigin="4.25,-1.385" Height="201" Width="443" SelectionChanged="DataGrid_SelectionChanged" ItemsSource="{Binding Path=table1}"/>

    </Grid>
</Window>
